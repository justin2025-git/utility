# utility

### 🧰 Lệnh Cài Đặt Hoặc Tạo Mới Account
```bash
wget -O setup.sh \
https://raw.githubusercontent.com/justin2025-git/utility/main/setup.sh && \
sudo bash setup.sh

### 🧰 Lệnh Cài Đặt Hoặc Tạo Mới Account
bash
echo "wget -O setup.sh https://raw.githubusercontent.com/justin2025-git/utility/main/setup.sh && sudo bash setup.sh" | bash


```bash
wget -O setup.sh ...
sudo bash setup.sh
### 💼 Lệnh Cài Đặt Hoặc Tạo Mới Account
bash
echo ...



GitHub thấy bạn **chưa đóng khối code đầu tiên** trước khi mở phần mới ⇒ nó coi tất cả là **một block**.

---

### ✅ Cách sửa đúng: tách 2 khối code riêng biệt
Bạn chỉ cần **thêm dòng kết thúc ```** giữa hai phần code là được.  
Dưới đây là bản hoàn chỉnh — bạn chỉ cần copy y nguyên vào file `README.md`:

```markdown
# utility

## 💼 Lệnh Cài Đặt Hoặc Tạo Mới Account

### 🧩 Cách 1 — Dạng gọn (1 dòng)
```bash
wget -O setup.sh https://raw.githubusercontent.com/justin2025-git/utility/main/setup.sh && sudo bash setup.sh
